// V2-NEW (reversible): kill-switch de toda la UI V2.
// Para volver a la interfaz anterior: poner en false (o borrar el bloque
// marcado V2-NEW en App.tsx + esta carpeta src/components/v2/).
// P-F (16-sep-2026): panel de depuración oculto por defecto; solo visible
// en desarrollo (V2_ENABLED = true).
export const V2_ENABLED = false;
