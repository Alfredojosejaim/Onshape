# Plan de ejecución — huecos del inventario

Basado en `inventario_motores_herramientas.md`. Orden por dependencia real y relación riesgo/beneficio, no por orden en que aparecen en el inventario. Sigue tu flujo: Claude arma diff/spec → vos aprobás → Muse Spark ejecuta vía ECC (`/plan` → aprobar → TDD → `/code-review` → `/build-fix`).

---

## Fase 0 — Higiene inmediata (antes que nada, riesgo casi cero)

**0.1 Ocultar/deshabilitar ESO y Level-Set en cualquier selector de UI**
Si `OptimizerType.ESO` / `LEVEL_SET` aparecen como opciones elegibles en algún combo, eso viola tu propio principio de "no fallback silencioso": el usuario elegiría un motor que no existe y probablemente caería en SIMP sin avisar, o rompería. Fix: restringir el combo a `SIMP` únicamente, o si ya están ocultos, no hace falta tocar nada — solo confirmar.
- Riesgo: nulo. Candidato ideal para Muse Spark sin supervisión estrecha.
- No depende de nada.

**0.2 Confirmar que `ObjectiveType.MINIMIZE_VOLUME_SUBJECT_TO_COMPLIANCE` no es seleccionable en UI**
Mismo motivo: el solver la rechaza explícitamente (`topo_problem.py:362-365`), así que si es elegible en algún panel, es una trampa silenciosa disfrazada de opción válida.
- Riesgo: nulo.

---

## Fase 1 — Exponer lo que YA funciona en backend (mayor ROI, menor riesgo)

Estos tres ítems no requieren tocar ningún solver: el motor ya está validado y corre. El trabajo es UI + wiring, no matemática.

**1.1 Panel de estudio Térmico**
- Agregar `ThermalAnalysis` a `study_panel.py` (mismo patrón que `TopologyOptimizationStudy`)
- Exponer `ThermalBoundary` (temperatura / flujo / convección) como condición seleccionable, reusando `NodeSelectionEngine` que ya tenés
- Conectar al `execute_on_mesh()` que ya existe en `cae_studies.py`
- Resultado esperado: temperatura nodal + flujo por elemento visibles en `results.py`

**1.2 Panel de estudio Modal**
- Mismo patrón: agregar `ModalAnalysis` a `study_panel.py`
- UI mínima: cantidad de modos, frecuencia mín/máx (`ModalParameters` ya existe)
- Requiere que el usuario defina al menos una fijación (ya validado en `validate_with_message()` — exige constraints para evitar K singular)
- Resultado esperado: lista de frecuencias + posibilidad futura de animar modo (la animación queda para después, no bloquea esta fase)

**1.3 UI de `load_case_id` en el panel de Carga**
- Agregar campo opcional "caso de carga" (agrupador) + peso relativo a `LoadCondition`
- Sin este campo, todas las cargas caen en el mismo caso (`__single_{n}` en `controller.py`) — comportamiento actual, no rompe nada, es aditivo
- Esto activa multicarga real sin tocar el solver

**Orden interno sugerido dentro de la fase:** 1.3 primero (más chico, un solo campo nuevo), después 1.1 y 1.2 en paralelo si querés repartir entre sesiones — no dependen entre sí.

**Riesgo:** bajo-medio. Es el primer tramo de esta fase donde recomendaría diff revisado por vos antes de aplicar (no delegación ciega a Muse Spark), porque toca paneles con más superficie de UI que los bugs P1-P4.

---

## Fase 2 — Bugs conocidos (ya tenés el orden, lo mantengo sin cambios)

Tu cadena ya establecida: **P1 → P2 → P4 → P3**

- **P1** — Correspondencia de tags OCCT↔Gmsh (`face_correspondence.py` ya tiene el mecanismo de `FaceSignature`; falta conectarlo en `GmshTet4Mesher._extract_all_surface_elements()`)
- **P2** — Propagación de `face_id` a triángulos de frontera (depende de P1 resuelto)
- **P4** — Halo derivado de `filter_radius` en vez de tamaño de elemento real (independiente, buen candidato de bajo riesgo para probar el pipeline ECC)
- **P3** — Semántica de `volfrac_mode` + detección de infeasibility explícita

No repito el detalle porque ya está fijado; solo lo ubico en la secuencia general porque P1/P2 bloquean que la Fase 1.1 (térmico) tenga distribución de flujo confiable si algún día usás caras para condiciones de contorno térmicas por flujo — ahí sí hay dependencia cruzada: si vas a exponer `HEAT_FLUX`/`CONVECTION` en 1.1 con selección por cara, conviene tener P1 resuelto antes, porque ambos dependen del mismo mapeo cara↔malla. Si en 1.1 solo usás `TEMPERATURE` (por nodos) al principio, podés adelantar la Fase 1 sin esperar P1.

---

## Fase 3 — Postproceso nuevo (autocontenido, sin dependencias del solver)

**3.1 Factor de seguridad**
- Función derivada: `FoS = yield_strength / von_mises` por elemento
- Input: resultado de `StructuralAnalysis` (ya tenés von Mises) + `Material.yield_strength` (ya existe)
- Visualización: mapa de zonas bajo el mínimo, reusando el mismo pipeline de color que ya usás para tensión
- No toca ningún solver, es post-cálculo puro

**3.2 Comparación de resultados (original vs. optimizado)**
- Estructura de datos nueva: snapshot de {masa, volumen, compliance, desplazamiento máx, FoS mín} por estudio
- UI: tabla comparativa simple primero (A vs B), sin animaciones ni gráficos todavía
- Extensible después a A vs B vs C

**Riesgo:** bajo. Buenos candidatos para Muse Spark con TDD, ninguno toca código de solver existente.

---

## Fase 4 — MMA/GCMMA (mayor esfuerzo, ya está en tu roadmap)

- Conectar `KratosOptimizationApplication` (MMA/GCMMA) al núcleo SIMP, replicando el patrón dual-backend que ya usás en FEA (`fea_solver` inyectable en `VendoredSIMP`, visto en `api.py: _simp_loop`)
- Esto es lo único de esta lista que yo trataría con diff-first estricto y revisión tuya en cada paso, no delegación amplia a Muse Spark — toca el núcleo del optimizador
- Depende de: nada técnicamente bloqueante de fases anteriores, pero tiene sentido hacerlo después de estabilizar Fase 1-3 para no mezclar cambios de UI con cambios de solver en la misma ventana de trabajo

---

## Fase 5 — Herramientas de malla nuevas (remesh, decimación, reparación avanzada)

- Remallado, decimación y reparación no-manifold/self-intersections no existen aún
- Recomendación: no arrancar esta fase hasta que P1/P2 estén cerrados — cualquier herramienta nueva de malla que dependa de correspondencia cara↔malla hereda el mismo riesgo que ya tenés diagnosticado
- Prioridad relativa baja: no bloquea ningún flujo actual, es calidad de geometría de salida

---

## Fase 6 — Largo plazo (cerrada 6a–6f, 13-sep-2026)

- Restricción de tensión máxima ✅ (6b): ESO con `eso_criterion="stress"` (von Mises por elemento, Xie & Steven original) + `max_von_mises` y `p_norm_von_mises` en todo resultado.
- Manufacturing constraints ✅ parcial (6c): simetría por planos (`symmetry_planes`, espejo-exacto en OC/ESO) + `overhang_report` diagnóstico por facetas + `getOverhangReport`. Overhang *activo* (sensibilidades de soporte) y resto (espesor mínimo = `filter_radius` ya existente) documentados como fuera de alcance.
- Acoplamiento térmico-estructural ✅ (6d): `thermal_load_vector` one-way (f_e = V·Bᵀ·D·ε_th) + wiring controller/engine/api (`thermal_temperatures`/`thermal_alpha`/`thermal_reference_temperature`).
- ESO ✅ motor real (Fase 6a) / Level-Set ✅ motor real (Fase 6f, HJ explícito + redistancing + nucleación topológica).
- Animación de modos ✅ (6e): `animate_mode_shape` + `getModeAnimation` (job modal + malla activa).

---

## Resumen de secuencia recomendada

```
Fase 0 (higiene)
   ↓
Fase 1.3 (UI load_case_id) ──┐
Fase 1.1 (panel térmico)      ├─ pueden repartirse, no dependen entre sí
Fase 1.2 (panel modal)       ─┘
   ↓
Fase 2 (P1 → P2 → P4 → P3)   — ya en curso según tu cadena existente
   ↓
Fase 3 (FoS + comparación)   — puede adelantarse en paralelo a Fase 2, es independiente
   ↓
Fase 4 (MMA/GCMMA)
   ↓
Fase 5 (malla nueva)
   ↓
Fase 6 (futuro sin código)
```

Nota: Fase 3 es realmente paralela a Fase 2 — no hay dependencia técnica entre postproceso nuevo y los bugs de mallado. Si en algún momento querés repartir carga entre dos sesiones de trabajo, esa es la pareja natural para hacerlo en simultáneo.

---

## Bitácora de avance (12-sep-2026)

- **Fase 0 ✅** (`ab95be6`): saneados combos de `properties.py` (solo Compliance mínima + SIMP/OC). ESO/Level-Set/MMA/GCMMA/Steepest sin motor no son elegibles.
- **Fase 1 ✅** (`ab95be6`): Thermal/Modal en `StudyPanel` desktop (el controller ya los ejecutaba; React ya los tenía vía V2Panel) + `load_case_id`/`load_weight` en UI React de Carga (backend ya agrupaba por metadata).
- **Fase 2 ✅** (`8499e02`): P1/P2/P4 verificados ya resueltos con malla real (`cono.step`: correspondence deterministic, sin unmapped; halo desde malla). P3 implementado: `problem_to_solver_inputs` rechaza infactibilidad por volumen (KEEP_IN/FROZEN_FACE > volfrac).
- **Fase 3 ✅** (`e3d431a`): FoS por elemento (`factor_of_safety`, `safety_summary`, campo `safety` en `getSurfaceMesh`, `getSafetySummary`) + `compareStudies`/`study_snapshot`; UI `SafetyCard` + `CompareTable` (reversibles).
- **Fase 4 ✅ (cerrada 13-sep-2026)**: Kratos 10.4 no expone MMA standalone → MMA propio numpy en `core/topopt.py` (`optimizer="mma"`, OC default intacto, 30 iters cantilever: MMA 0.7506 vs OC 0.7856, vol exacto). Cableado completo: (a) `controller.run_optimization(optimizer=...)` en ambas ramas + `OptimizerType.MMA`; (b) `api.runOptimization`/`runSimpLoop` aceptan `optimizer` (rechazo explícito, sin fallback) + espejo MMA en `vendored/simp.py` + `generative_engine.run_generative_design` propaga optimizer del estudio; (c) selector MMA en UI desktop (`properties.py` combo OC/MMA → `main_window.py` → controller). Verificado: `py_compile` OK + `vendored` rechaza `optimizer='bogus'` con `TopOptError` + firmas API contienen `optimizer`.
- **Fase 5 ✅ (cerrada 5a+5b, 13-sep-2026)**: herramientas de malla numpy-only en `core/cad_reconstruction.py`, sin dependencias nuevas. 5a: `mesh_quality_report`, `repair_mesh`, `decimate_mesh`, `MeshRepair`/`MeshDecimator`, wiring opt-in en `ReconstructionPipeline`. 5b: `uniform_remesh` (split >4/3·L / collapse <4/5·L / flip por valencia + laplaciano con borde fijo + `repair_mesh` final, clase `MeshRemesher`), `split_non_manifold` (duplicación explícita por hoja/componente, residual 0, sin eliminar geometría), `count_self_intersections` (broadphase por grilla + SAT completo, solo REPORTA — la reparación es ambigua por definición). Verificado sintético: remesh 200→29 tris hacia L=0.2 sin degenerados; split deja residual 0; SAT detecta 1 cruce real y 0 falsos en plano (tras corregir ejes coplanares + celda por área). Siguiente: Fase 6 (largo plazo, sin código todavía).
- **Fase 6a ✅ (13-sep-2026)**: ESO hard-kill real en `core/topopt.py` (`optimizer="eso"`, `_eso_optimize`: ranking por energía de deformación filtrada + promedio histórico, tasa evolutiva ER=0.02, preservados nunca removidos, convergencia clásica por estabilidad del compliance + óptimo discreto). Refactor seguro: análisis final extraído a `_finalize_result` compartido (OC/MMA bit-idénticos: regresión cantilever 10 iters OC C=1030 / MMA C=715.2, vol exacto). Espejo en `vendored/simp.py` + wiring (`controller`, `api.runOptimization`/`runSimpLoop`, `generative_engine` con mapeo honesto sin coerción silenciosa, UI desktop OC/MMA/ESO). Verificado cantilever 36 tets: ESO 7 iters, diseño binario 0/1, vol 0.5005 (tolerancia de un elemento discreto), core y vendored idénticos. Resta Fase 6: tensión máxima, manufacturing, acoplamiento térmico-estructural, Level-Set, animación de modos.
- **Fase 6 ✅ (cerrada 6b–6f, 13-sep-2026)**: (6b) ESO `eso_criterion="stress"` + `max/p_norm_von_mises` en todo resultado (cantilever: stress-ESO binario 7 iters, core=vendored); (6c) `set_symmetry_planes` (espejo-exacto OC/ESO verificado) + `overhang_report` por facetas (`T: 0.242` vs `columna: 0.0`) + `api.getOverhangReport`; (6d) `thermal_load_vector` one-way auto-equilibrado (ΣF≈1e-13, dT=0→cero exacto) + wiring controller/engine/api, verificado acoplado (C 2.05e9→9.2e9 con gradiente); (6e) `animate_mode_shape` (auto=5% bbox, sinusoidal exacto, eigenvector real `solve_modal`) + `api.getModeAnimation`; (6f) Level-Set HJ (`optimizer="level_set"`, Heaviside 0.5h, λ con ganancia, nucleación por espaciado nodal, Sussman): cantilever 12×2×2 → vol 0.47, C 8.7e4 (rango OC), conectado; delegación vendored→núcleo con etiqueta engine y error propio. Bugs encontrados y corregidos: signo de velocidad HJ, radio de nucleación bajo espaciado nodal, SAT coplanar, grilla plana, soporte circular en overhang. Plan completo: Fases 0–6 cerradas.
- **Fase 4.5 ✅ (auditoría scope-creep, 14-sep-2026)**: 4.5a verificado — `load_case_id`/`load_weight` ya en UI React (`faces.ts` + `ToolParamsPanel` + `App`); desktop cerrado en 2da ronda (campo ID + peso en Cargas). 4.5b decisión EXPLÍCITA (revertir): `vendored/simp.py` vuelve a CONGELADO, simetría solo en núcleo (ver `AGENTS.md`). 4.5c `backend/tests/test_fase45_regresion.py` 9/9 (simetría+rechazo vendored, térmico auto-equilibrado/cero-exacto, ESO binario compliance+stress, level-set estable, animación forma/salida). 4.5d `evolutionary_rate`/`ls_cfl`/`ls_hole_period` y plano de simetría expuestos en desktop (`properties.py` → `main_window.py` → `controller` → `generative_engine`/`api.runOptimization`/`runSimpLoop`/`_simp_loop`, con validación fail-loud en cada capa). Diferido con motivo: toggle térmico (requiere estudio térmico previo resuelto como fuente de T) y botón animar-modo (requiere plumbing de resultados modales al panel) — ambos necesitan flujo de datos, no solo un campo. 0.5 confirm-gate documentado en `AGENTS.md`.
- **Fase 4.5d.4 ✅ + 4.5d.5 ✅ (14-sep-2026)**: toggle térmico (properties + StudyPanel → `resolve_thermal_kwargs` → ambos paths, vale core+vendored) y animar-modo (results + QTimer + 5 restricciones VTK). Tests 16/16. Detalle en `docs/cierre-fases-4b-6f.md` §12.
- **Fase 6 GCMMA ✅ (14-sep-2026, solo núcleo, vendored intacto)**: `optimizer="gcmma"` en core + controller/api/engine/UI; benchmark Kuhn OC=202.6 MMA=124.2 GCMMA=138.5. Tests 20/20. Detalle en `docs/cierre-fases-4b-6f.md` §13.
- **Cierre prompt.md (14-sep-2026, auditoría + validación pre-export)**: verificado que GCMMA/ESO/Level-Set/simetría/multicarga/térmico/modal/generativo A+B ya están operativos UI+backend (tests 13/13: `test_gcmma` + `test_fase45_regresion`). Añadido `api.validateExportGeometry` (Fase 5+6c, solo diagnóstico: `mesh_quality_report` + `count_self_intersections` + `overhang_report` volumétrico si hay densidades; sin malla o con degenerados falla explícito). Fuera de plan y SIN implementar sin aprobación explícita (confirm-gate): espesor mínimo independiente, overhang activo, reparación auto de self-intersections, minimizar-volumen-sujeto-a-compliance, Tet10/Hex8, soportes automáticos.
- **Implementación alcance prompt.md ✅ (14-sep-2026, con aprobación explícita del usuario)**: (a) espesor mínimo `min_thickness` en `core/topopt.py` (piso de filtro + apertura morfológica por iteración, fail-loud si ≤0); (b) overhang activo `overhang_constraint` (filtro por capas según `build_direction`/ángulo, penalización configurable); (c) `repair_self_intersections` (weld + laplaciano local iterado, reporta residual) + `api.repairSelfIntersections`; (d) soportes `generate_supports` (pilares a la base desde `unsupported_ids`) + `api.generateSupports`; (e) objetivo `min_volume` por bisección externa sobre volfrac (requiere `compliance_limit`, fail-loud si infactible; `topo_problem` acepta `MINIMIZE_VOLUME_SUBJECT_TO_COMPLIANCE` con `max_compliance`); (f) Tet10 (`tet4_to_tet10` + `solve_fea_tet10` por subdivisión en 8 Tet4) y Hex8 (`hex8_stiffness` Gauss 2x2x2 + `solve_fea_hex8`); wiring `controller`/`generative_engine`/`api.runOptimization`/UI desktop (espesor, overhang, objetivo+limite). Tests: `test_fabricacion_objetivo.py` 5/5, suite total 41/41. Vendored intacto (sin gcmma/simetría/nuevos objetivos por path vendored).
- **Auditoría + cierre flujo generativo (14-sep-2026)**: auditoría del proyecto y arreglo del reporte "la optimización generativa no hace nada". Hallazgos corregidos:
  - **Suite rota**: `test_fabricacion_objetivo.py:59` tenía `def` e `import` en la misma línea (IndentationError) → `pytest` no colectaba NADA. Corregido; la suite real subió de "1 error de colección" a 42/42.
  - **No-op silencioso (GEN-LEGACY/GEN-NOCOND)** en `core/generative_engine.py`: a diferencia de `controller.run_optimization`, la generativa no caía a las BC clásicas (`self.forces`/`self.constraints`) y resolvía con carga cero. Ahora `run_generative_design` acepta `legacy_force`/`legacy_fixed_dofs` y `api.runGenerativeDesign` los toma de `controller.build_problem()` cuando no hay `condition_ids`; si tampoco hay BC, **falla explícito** (nunca un no-op).
  - **Escenario B**: `run_generative_design` construía la malla puente solo si `engine.mesh_nodes is None`, que nunca ocurre (el controller inyecta la malla) → B optimizaba la pieza A en silencio. Ahora B siempre construye la puente desde la malla del modelo.
  - **Frontend (STARTUP-NOIMPORT)**: `App.tsx` auto-importaba `fixtures[0]` (cono.step) en cada arranque, pisando el modelo del usuario y haciendo que los estudios corrieran sobre geometría ajena a la app. Ahora solo lista los fixtures; el usuario elige/sube.
  - **Frontend (MULTI-KEY)**: `handleSelectModelReal` re-importaba por `filename` (falla para archivos subidos, que no están en las carpetas de fixtures). Ahora guarda la `key` de librería del backend y usa `switchModel`.
  - Verificación: `test_generativa_flujo.py` 3/3 (condiciones reales, fail-loud, fallback legacy) + suite total **45/45**; `tsc --noEmit` limpio; `npm run build` OK y `dist/` reconstruido (el `.bat` servía un bundle previo). Reproducido con `1pieza.step` real (subida por bytes, caras reales): escenario A termina `done`, reconstrucción `brep_solid` y registro de modelo OK.
- **Fix visual generativa (GEN-TESS, 14-sep-2026)**: la reconstrucción se generaba bien (`brep_solid` válido, 108 caras) pero **no se mostraba**: `controller._register_reconstruction_model` validaba `tess.get("success")`, clave que el dict de ÉXITO de `tessellate_model` no trae (solo la trae el fallo) → `current_tessellation` quedaba `None` y el viewport seguía con la pieza original ("no modifica la pieza"). Ahora valida por geometría real (`vertices`/`indices`), igual que `import_model`. Verificado con `1pieza.step`: tras `registerReconstruction`, `getMeshPreview` devuelve el teselado de la pieza generada (no None). Regresión: `test_registration_exposes_reconstructed_tessellation` (integración API con `cono.step`); suite **46/46**.
- **Vista de densidades SIMP (DENSITY-VIEW, 15-sep-2026, a pedido del usuario)**: el SIMP estructural solo devolvia numeros (la pieza no cambiaba en el viewport). Solo visual, sin geometria nueva: tras el job estructural la UI trae `getSurfaceMesh({field:'density'})` (endpoint ya existente, con whitelist) y `CadViewport` pinta un overlay con vertex colors (vacio #1e3a8a -> solido #f97316) + toggle y leyenda en `RightPanel`. Limpieza en reset/nuevo modelo/remallado/nueva corrida.
- **PENDIENTE fuera de plan (requiere aprobacion explicita, confirm-gate)**: cablear el resultado SIMP estructural a reconstruccion iso-superficie registrada como modelo nuevo (como hace la via generativa con `registerReconstruction`). A pedido del usuario queda registrado aqui sin implementar.
- **Fix UI desktop generativa (15-sep-2026, con aprobacion explicita, dentro de plan: continuacion flujo generativo/GEN-TESS)**: porteados los dos diffs externos (rutas y lineas desfasadas, porteo manual). `controller._register_reconstruction_model`: `last_reconstruction_warning` consultable + `reconstruction["registration_error"]` en los 3 caminos `{}` (sin solido / sin store / excepcion con `logger.error` + traceback); camino de exito y validacion GEN-TESS intactos. `main_window._finish_study_execution`: si `reconstruction.model_id`, swap de viewport (`_show_tessellation` + `set_bodies` + `set_features`, patron de `_on_boolean_done`); si no, `QMessageBox` "Reconstruccion incompleta" con motivo exacto + statusBar. Verificado: `py_compile` OK, suite **46/46**, y el dict llega mutado in-place hasta `sr.data`. Nota: esto es la UI desktop Qt; la UI React (pywebview) ya mostraba la generativa via GEN-SHOW/GEN-TESS. Diagnostico en vivo pendiente: proximo run generativo clasifica Caso A (solido actualizado, era solo UI) vs Caso B (motivo OCP exacto, follow-up de calidad B-Rep).
- **Fallbacks explicitos generativa (15-sep-2026, dentro de plan: P1/P2 + principio explicito)**: porteado `generative_engine_fix.diff` (rutas `Topologia_Optimizada/core/...` vs reales `backend/core/...`, porteo manual). `_protected_elements` devuelve `(elems, used_bbox_fallback)`; cara seleccionada sin nodos (tolerance 0.5mm) en modo permisivo agrega `protected_region(fallback_bbox)` a unsupported (antes solo `logger.warning`); `ElasticityCondition` igual con `elasticity(fallback_base_Z)` + warning (antes totalmente silencioso). Ambos fluyen por `result._unsupported_conditions` al dialogo "Condiciones no soportadas" existente. Verificado: `py_compile` OK, suite **47/47** (regresion nueva en `test_generativa_flujo.py`: caras sin CAD shape quedan flaggeadas, sin caras no hay flags). El diff `test_resolved_pendientes_fix.diff` no tenia objetivo (`tests/test_resolved_pendientes.py` no existe) y se cubrio con esa regresion. NO toca el tolerance 0.5mm en si (4 sitios hardcodeados): diagnostico primero, decision de tolerancia mesh-adaptativa despues (conecta con P4/halo).
- **Validar antes de guardar + colapso compliance-0 (15-sep-2026, dentro de plan: P1/P2 + Caso B)**: el "cuerpo de 0,0 cm³" tras generativa era un sólido invertido/degenerado registrado sin validar (`store_computed_shape` valida cero; reproducido en cono.step: Volume()=-231 válido según BRepCheck). (a) `_validate_reconstruction_solid` en controller: BRepCheck + volumen positivo + >=1% del volumen de malla; inversión simple se repara con `Reverse()` explícito (flag `orientation_fixed`), lo demás se rechaza con `registration_error` (camino del día anterior). `_mesh_volume` suma absoluta por tet (la suma signada se cancela a 0 en mallas Kuhn). (b) Causa raíz del colapso: carga sobre nodos fijos (o caras sin mapear) → compliance 0 → OC colapsa todo a xmin (final_vol 0.001 con volfrac 0.4) → sliver. (c) Flag `load(fallback_bbox)` en `_force_vector_for_load` permisivo (el caso se conserva, solo se marca; los flags duros siguen descartando). (d) Test GEN-TESS pasó a dirección +Z (escenario físico válido; -Z caía sobre el soporte). Suite **48/48**. Observado sin tocar: `api._model_stats` usa suma signada (misma familia, solo display).
- **Motivo visible en React + flag load + colapso documentado (15-sep-2026)**: el aviso "sin geometría registrable" escondía el motivo. (a) `_reconstruct` expone `brep_error` (el dict final solo trae la mejor etapa; el error BREP se perdía). (b) Fuente única `reconstruction_failure_reason()` (controller; usada en controller/desktop-Qt/`api.registerReconstruction.reason`): corrige que el código leía `stages.brep_solid`, clave que el dict final NUNCA trae. (c) React muestra motivo + `_unsupported_conditions` (estructural y generativa). (d) Flag `load(fallback_bbox)` en permisivo (el caso se conserva). (e) Causa raíz del colapso verificada: carga sobre nodos fijos → compliance 0.0 → OC a xmin (final_vol 0.001 con volfrac 0.4); test GEN-TESS a +Z. Incidente: docstring pegado a la firma (suite en la línea del def no continúa; IndentationError) — reparado. Suite **49/49**, `tsc` limpio, `dist/` reconstruida.
- **Geometría limpia: Heaviside + extrusión 2D + Taubin + B-spline (15-sep-2026, FUERA DE PLAN con aprobación EXPLÍCITA del usuario, confirm-gate)**: tras auditar que el diagnóstico original ("falta filtro de densidad") era falso (el filtro H=rmin−dist ya existía en `topopt.py`), el usuario aprobó los 4 puntos realmente ausentes. (a) **Proyección Heaviside SIMP** (`topopt.py`: `set_heaviside_projection(beta,eta,continuation)`, `_physical_density`/`_project`/`_dproject`/`_design_sensitivity` con regla de la cadena filtro-densidad+proyección; continuación β=1→β cada 5 iters). Se eliminó el rechazo de `topo_problem.py:375`; se expone `heaviside_projection`/`heaviside_beta` en el dict de solver inputs. Sin activarla el lazo es bit-idéntico (regresión). (b) **Filtro de extrusión 2D** (`set_extrusion_filter(axis)`/`_apply_extrusion`): densidad constante por columna perpendicular al eje, para mecanizado/láser. (c) **Taubin** (`cad_reconstruction.taubin_smooth` λ|μ, sin encogimiento; `MeshSmoother(method=...)`, `ReconstructionPipeline(smoothing_method=...)`). (d) **Reconstrucción B-spline** (`OCPBSplineFitter`): sobre el sólido cosido aplica `ShapeUpgrade_UnifySameDomain` (fusiona coplanares) + `ShapeCustom_BSplineRestriction` + `ShapeUpgrade_ShapeDivideContinuity` C1/C2, validando cada paso y exportando STEP, con metadatos explícitos por paso y fallback al último sólido válido. Wiring: `controller.run_optimization`, `generative_engine.run_generative_design`/`_reconstruct`, `api.runOptimization`/`runGenerativeDesign` (validación fail-loud), desktop Qt (properties+main_window) y React (types+App+RightPanel: Heaviside/beta, extrusión 2D, STEP B-spline). Vendored CONGELADO: `runSimpLoop` rechaza heaviside/extrusión explícito. Tests nuevos: `test_heaviside_extrusion.py` (9) + `test_reconstruction_bspline.py` (4). Suite **67/67**, `tsc` limpio, `dist/` reconstruida.
- **Generativa orgánica: design space envelope + halo consistente + tolerancia adaptativa (15-sep-2026, aprobación EXPLÍCITA)**: diagnóstico con `1pieza.step` real (2114 nodos/10914 tets): (a) el mapeo cara→nodo a 3mm funciona (35/35 caras, dmin 0.0) — la tolerancia 0.5mm NO era el fallo; (b) el halo 1-capa SÍ preserva las zonas BC (densidad 1.0), pero el dominio de diseño era la pieza original → solo se puede vaciar por dentro → aspecto "esculpido", no truss; (c) la reconstrucción producía 10 shells y descartaba los chicos (riesgo de perder islas BC). Fixes: (1) **design space envelope** (`generate_design_space_mesh` + `design_space="box"` en `run_generative_design`, opt-in con default UI "envelope"): malla una caja que encierra la pieza (voxels→6 tets, res `max(span)/30`, cap 12000 voxels) donde el optimizador CRECE estructura; con envelope se desactiva `face_surface_elements` (`_surface_matches_mesh=False`, distribución uniforme honesta) y la tolerancia pasa a `1.5·h`; (2) **halo consistente** (`_resolved_load_nodes`/`_resolved_support_nodes`): el halo usa los MISMOS nodos BC que aplican carga/fijación (con fallback base-Z/extremo), antes `_support_node_indices` devolvía vacío y la zona BC quedaba sin proteger; (3) **tolerancia mesh-adaptativa** `_face_tolerance` (los 4 sitios hardcodeados 0.5 → atributo, decisión diferida en plan.md ahora tomada); (4) **multi-shell**: `OCPBRepFitter` conserva TODAS las cáscaras ≥1% de la mayor (antes solo la mayor) con fallback a la mayor si el sólido queda inválido, reportando `kept_shells`. Verificado end-to-end `1pieza.step` envelope: 60k tets, 20 iters/97s, Heaviside → **0 densidades grises** (100% 0/1), STEP B-spline `bspline_restriction=applied` + `continuity_upgrade=C1`. Tests nuevos `test_design_space_halo.py` (4). Suite **71/71**, `tsc` limpio, `dist/` reconstruida. Nota de performance: envelope ~5× más caro que la pieza (60k tets); bajar iteraciones o resolución si molesta.
- **Fix borrar/deshacer tras generativa (15-sep-2026, bug reportado)**: "las piezas no se eliminan; si se eliminan se rompe el pipeline y no permite más de una optimización; hay que reiniciar la app". Causa raíz: `_register_reconstruction_model` hacía de la pieza reconstruida el modelo ACTIVO del controller (`model_id` nuevo) pero **no entraba en `api._library`** (el frontend la recibía sin `key`), así que `handleDeleteSelected` no tenía key para `removeModel`; además la optimización **no era deshacible** (la pila undo solo tenía removeModel/deleteCondition) y el controller quedaba con `model_id` nuevo + malla/condiciones viejas (2da corrida inconsistente). Fix `api.registerReconstruction` (LIBRARY-FIRST): exporta el sólido computado a STEP en `uploads/computed_<id>.step`, lo registra en la librería (`_register_library` → key), lo re-importa (`import_model` → `close_model` limpia malla/condiciones del modelo anterior, dejando el pipeline listo para otra corrida) y **empuja un undo** (`_undo_recon`/`_redo_recon`, con copia de la entrada para rehacer) que restaura el modelo original con Ctrl+Z. React: GEN-SHOW usa `rr.entry`/`rr.key` para `applySnapshotToModel`. Regresión ampliada en `test_registration_exposes_reconstructed_tessellation` (key en librería, borrable, undo restaura el original, redo reactiva). Suite **71/71**, `tsc` limpio, `dist/` reconstruida.
- **Árbol: selección con clic simple + borrado de herramientas (15-sep-2026, pedido del usuario)**: antes el clic simple abría la herramienta y no había forma de borrarla desde el árbol. `OperationRow`: clic simple solo MARCA (resalta con ring/borde), doble clic ABRE la herramienta, y se agregó botón papelera (`onDelete`). `LeftPanel` propaga `selectedConditionId`/`onSelectCondition`/`onDeleteCondition`. `App`: estado `selectedCondId`, `handleSelectCondition` y Supr prioriza la fila marcada (con `deleteSelectedRef` para no usar closures obsoletas). `tsc` limpio, `dist/` reconstruida.
- **Ajuste árbol + endurecer framework bridge (15-sep-2026, pedido del usuario)**: se QUITÓ la papelera de las herramientas (el borrado queda con Supr sobre la fila marcada). Diagnóstico del "error de url / falta de conexión" al borrar: verificado que (a) `server.py` responde bien `deleteCondition`/`removeModel` incluso tras import→malla→optimización, y (b) pywebview 6.2.1 expone los 48 métodos (incl. `deleteCondition`) vía `get_functions`; la causa es el TRANSPORTE (server caído o instancia vieja), no el método. Endurecido: `server.py` → `ThreadingHTTPServer` (una request larga ya no bloquea el resto) + log de fallos a `server_error.log`; `app_desktop.py` `_call` devuelve `{ok:false,error:"backend no responde (...)"}` en vez de propagar `URLError` (que se veía como error de conexión); `bridge.ts` `call` valida que el método exista antes de invocarlo (error explícito, no TypeError opaco). Suite **71/71**, `tsc` limpio, `dist/` reconstruida.
- **"Ahora no itera" (15-sep-2026)**: reproducido por API; el backend itera bien en todos los casos (structural Heaviside: progreso 0→10 en 1.8s; generativa `1pieza` con caras reales + envelope + B-spline: 8 iteraciones, 0.125→1.0, 76.6s). Causa: el **design space 'envelope' quedó por defecto** y malla ~60k tets → la 1ra iteración tarda ~10s+ con progreso 0, y el polling era cada 2s → parecía colgado. Fix: default UI `designSpace: 'part'` (itera de inmediato; el envelope queda como opción para orgánico), polling 2s→1s (`jobs.ts`), y aviso explícito al arrancar una generativa con envelope ("generando dominio de diseño…"). `tsc` limpio, `dist/` reconstruida.
- **Generativa sin geometría + "elasticity degradada" (15-sep-2026)**: causa raíz encontrada y reproducida: cuando la malla cae al **ProvisionalTet4Mesher (voxel, NO conforme al CAD)**, los nodos quedan a ~h/2 de las caras y el umbral fijo 0.5mm mapea 0-1 nodos → la fijación se marca degradada (`elasticity(fallback_base_Z)`) → la optimización colapsa → la isosuperficie sale abierta (`sewed_shell_volumes: [0.0,…]`, `kept_shells: 0`) → "reconstrucción no válida". Medido: provisional 1pieza a 0.5mm → 21/35 caras sin nodos; a 2·h (≈14.9mm) → 0/35. Fixes: (a) **tolerancia adaptativa con reintento** (`_select_nodes_for_faces`): 0.5mm estricto y, si el mapeo es DISPERSO (< ½ de los nodos esperados por área A/h²), reintenta con `2·h_elemento` y conserva el mapeo con más nodos (conforme no cambia; provisional ahora mapea 142/154 nodos, `unsupported: []`); (b) **reconstrucción robusta**: `ShapeFix_Solid` intenta cerrar/orientar la isosuperficie abierta y, si no, devuelve FAILED explícito con `sewed_shell_volumes` (nunca COMPLETED con `kept_shells: 0`); (c) UI: aviso cuando la malla salió del mesher provisional (no conforme). Regresión nueva en `test_design_space_halo.py` (mapeo adaptativo). Suite **72/72**, `tsc` limpio, `dist/` reconstruida.
- **85.3 cm³ → 1.7 cm³ (15-sep-2026, REGRESIÓN propia del framework)**: causa raíz encontrada en el log del server: `GmshTet4Mesher failed (signal only works in main thread of the main interpreter); falling back to ProvisionalTet4Mesher`. El `ThreadingHTTPServer` que agregué en el endurecimiento del bridge corría los handlers en hilos, y **Gmsh usa `signal`, que solo funciona en el hilo principal** → `generateMesh` caía al mesher provisional (voxel, NO conforme) → el dominio era una malla gruesa (2040 tets) y la reconstrucción salía mínima. Reproducido: con Threading el server logueaba Provisional (744 nodos) y reconstruía 0.9 cm³; con `HTTPServer` usa Gmsh (11212 elementos) y reconstruye **27.9 cm³** (33% de 85.3). Fix: **revertir `server.py` a `HTTPServer` mono-hilo** (comentario explícito de por qué NO volver a threading sin sacar gmsh del handler). Se conservan las mejoras defensivas (tolerancia adaptativa + reparación de isosuperficie) para cuando el provisional sea legítimo (gmsh ausente). Suite **72/72**.
