# App autocontenida: el core va vendorado en backend/core (+desktop,
# adapters, services). Este archivo es el cierre REAL de dependencias
# (medido por import estatico + modulos cargados en import/malla).
# Stack cientifico del core vendorizado:
numpy>=1.24.0
scipy>=1.11.0
cadquery>=2.3.0
gmsh>=4.13.0
# PipelineController importa PySide6.QtCore al arrancar (obligatorio):
PySide6>=6.6.0
# El flujo importar+mallar carga vtkmodules via cadquery (obligatorio):
vtk>=9.2.0
# Shell nativo (ventana WebView2, sin navegador):
pywebview>=4.4
# Solo backend kratos (opcional: sin el, el flujo local funciona y el
# verificado-Kratos reporta kratos_no_disponible):
# KratosMultiphysics==10.4.3
# KratosStructuralMechanicsApplication==10.4.3
# KratosOptimizationApplication==10.4.3
# Tests del experimento:
pytest>=7.4.0
